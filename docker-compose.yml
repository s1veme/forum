version: '3.8'
services:
  django:
    build: ./
    env_file:
      - .env
    command: python manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/usr/src/forum
    ports:
      - "8000:8000"
    networks:
      - forum_network

networks:
  forum_network:
    driver: bridge
